---
title: Thiết lập môi trường
description: Clone, build và chạy TablePro cục bộ với Xcode 15+, SwiftLint và SwiftFormat
---

# Thiết lập Phát triển

## Yêu cầu

### Phần mềm bắt buộc

| Phần mềm | Phiên bản | Mục đích |
|----------|---------|---------|
| **macOS** | 14.0+ (Sonoma) | Nền tảng target |
| **Xcode** | 15.0+ | IDE phát triển |
| **Git** | Latest | Quản lý phiên bản |

### Công cụ tùy chọn

| Công cụ | Mục đích |
|------|---------|
| **SwiftLint** | Code linting |
| **SwiftFormat** | Code formatting |
| **Homebrew** | Quản lý package |
| **GitHub CLI (`gh`)** | Tải thư viện native từ GitHub Releases |

## Bắt đầu

### Bước 1: Clone và Bootstrap

```bash
git clone https://github.com/datlechin/tablepro.git
cd tablepro

# Tải thư viện native từ GitHub Releases
scripts/download-libs.sh

# Tạo file config bắt buộc (file trống đủ cho phát triển)
touch Secrets.xcconfig

# Cài đặt công cụ lint/format
brew install swiftlint swiftformat
```

<Warning>
Bỏ qua `scripts/download-libs.sh` sẽ khiến `Libs/` không có binary, gây lỗi linker.
</Warning>

### Bước 2: Mở trong Xcode

```bash
open TablePro.xcodeproj
```

### Bước 3: Cấu hình Signing

1. Chọn target **TablePro** trong project navigator
2. Vào tab **Signing & Capabilities**
3. Đổi **Team** sang tài khoản Apple Developer của bạn

<Info>
Dự án sử dụng team ID của maintainer. Bạn cần chuyển sang team của mình để build. Tài khoản Apple Developer miễn phí cũng có thể dùng cho bản development.
</Info>

### Bước 4: Build và Run

1. Chọn scheme **TablePro**
2. Chọn **My Mac** làm destination
3. Nhấn `Cmd+R` để build và chạy

{/* Screenshot: Xcode with scheme selected */}
<Frame caption="Thiết lập dự án Xcode">
  <img
    className="block dark:hidden"
    src="/images/xcode-setup.png"
    alt="Xcode setup"
  />
  <img
    className="hidden dark:block"
    src="/images/xcode-setup-dark.png"
    alt="Xcode setup"
  />
</Frame>

## Cấu trúc dự án

<Tree>
  <Tree.Folder name="tablepro" defaultOpen>
    <Tree.Folder name="TablePro" defaultOpen>
      <Tree.Folder name="Core" defaultOpen>
        <Tree.Folder name="Database">
          <Tree.File name="DatabaseDriver.swift" />
          <Tree.File name="DatabaseManager.swift" />
        </Tree.Folder>
        <Tree.Folder name="Plugins">
          <Tree.File name="PluginDriverAdapter.swift" />
          <Tree.File name="PluginManager.swift" />
        </Tree.Folder>
        <Tree.Folder name="Services" />
        <Tree.Folder name="Utilities" />
      </Tree.Folder>
      <Tree.Folder name="Models" />
      <Tree.Folder name="Views">
        <Tree.Folder name="Connection" />
        <Tree.Folder name="Editor" />
        <Tree.Folder name="Main" />
        <Tree.Folder name="Results" />
        <Tree.Folder name="Settings" />
      </Tree.Folder>
      <Tree.Folder name="ViewModels" />
      <Tree.Folder name="Extensions" />
      <Tree.Folder name="Theme" />
      <Tree.Folder name="Resources" />
    </Tree.Folder>
    <Tree.Folder name="Plugins" defaultOpen>
      <Tree.Folder name="TableProPluginKit" />
      <Tree.Folder name="MySQLDriverPlugin" />
      <Tree.Folder name="PostgreSQLDriverPlugin" />
      <Tree.Folder name="SQLiteDriverPlugin" />
      <Tree.Folder name="ClickHouseDriverPlugin" />
      <Tree.Folder name="..." />
    </Tree.Folder>
    <Tree.Folder name="TableProTests" />
    <Tree.Folder name="Libs" />
    <Tree.Folder name="scripts">
      <Tree.File name="build-release.sh" />
      <Tree.File name="create-dmg.sh" />
    </Tree.Folder>
    <Tree.File name=".swiftlint.yml" />
    <Tree.File name=".swiftformat" />
  </Tree.Folder>
</Tree>

## Build

### Build Phát Triển

```bash
# Build kiến trúc hiện tại (Debug)
xcodebuild -project TablePro.xcodeproj -scheme TablePro -configuration Debug build -skipPackagePluginValidation

# Hoặc trong Xcode: Cmd+B
```

### Build Release

```bash
# Apple Silicon
scripts/build-release.sh arm64

# Intel
scripts/build-release.sh x86_64

# Cả hai kiến trúc
scripts/build-release.sh both
```

Xem [Build](/vi/development/building) để biết chi tiết.

## Chạy Tests

```bash
# Tất cả tests
xcodebuild -project TablePro.xcodeproj -scheme TablePro test -skipPackagePluginValidation

# Test class cụ thể
xcodebuild -project TablePro.xcodeproj -scheme TablePro test -skipPackagePluginValidation \
    -only-testing:TableProTests/TestClassName

# Test method cụ thể
xcodebuild -project TablePro.xcodeproj -scheme TablePro test -skipPackagePluginValidation \
    -only-testing:TableProTests/TestClassName/testMethodName
```

Hoặc trong Xcode: **Product** > **Test** (`Cmd+U`).

## Chất lượng Code

### SwiftLint

```bash
# Kiểm tra vấn đề
swiftlint lint

# Tự động sửa
swiftlint --fix
```

SwiftLint cũng chạy tự động khi build Xcode.

### SwiftFormat

```bash
# Format tất cả code
swiftformat .

# Kiểm tra mà không áp dụng
swiftformat --lint .
```

<Tip>
Chạy SwiftFormat trước khi commit để giữ code style nhất quán.
</Tip>

## Tệp cấu hình

### .swiftlint.yml

Rules và cấu hình SwiftLint:

- Giới hạn độ dài dòng
- Giới hạn độ phức tạp function
- Rules bị vô hiệu hóa
- Rules tùy chỉnh

### .swiftformat

Tùy chọn SwiftFormat:

- Kiểu indentation
- Vị trí brace
- Thứ tự import
- Các rules formatting khác

## Workflow phát triển

### Thực hiện thay đổi

1. Tạo feature branch:
   ```bash
   git checkout -b feature/my-feature
   ```

2. Thực hiện thay đổi

3. Chạy linting:
   ```bash
   swiftlint lint
   swiftformat --lint .
   ```

4. Chạy tests:
   ```bash
   xcodebuild -project TablePro.xcodeproj -scheme TablePro test -skipPackagePluginValidation
   ```

5. Commit:
   ```bash
   git add .
   git commit -m "Add my feature"
   ```

6. Push và tạo pull request:
   ```bash
   git push origin feature/my-feature
   ```

### Debugging

1. Đặt breakpoints bằng cách click số dòng
2. Dùng `po` trong debug console để in objects
3. Dùng Variables View để kiểm tra state
4. Dùng Console.app cho system logs

### Logging

TablePro dùng OSLog:

```swift
import os

private static let logger = Logger(subsystem: "com.TablePro", category: "ComponentName")

// Sử dụng
logger.debug("Connection established")
logger.info("Query executed in \(duration)s")
logger.error("Failed to connect: \(error.localizedDescription)")
```

## Tác vụ thông dụng

### Thêm View mới

1. Tạo SwiftUI view trong subfolder `Views/` phù hợp
2. Theo quy ước đặt tên (`FeatureNameView.swift`)
3. Dùng `@Observable` hoặc `@StateObject` cho state management
4. Thêm vào navigation/routing nếu cần

### Thêm tính năng Database

1. Tạo plugin bundle mới trong `Plugins/`, hoặc sửa driver plugin hiện có
2. Triển khai các method trong protocol `PluginDatabaseDriver`
3. Cập nhật `PluginDriverAdapter` nếu cần bridge method mới
4. Thêm UI trong view components liên quan
5. Thêm tests

### Sửa đổi Settings

1. Thêm vào struct settings trong `AppSettings.swift`
2. Cập nhật `SettingsView`
3. Dùng setting trong app qua `@AppStorage` hoặc settings manager

## Khắc Phục Sự Cố

### Lỗi Build

1. Clean build folder: `Cmd+Shift+K`
2. Xóa derived data:
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData
   ```
3. Đóng và mở lại Xcode
4. Kiểm tra cập nhật Xcode

### Lỗi SwiftLint

1. Đọc error message
2. Kiểm tra `.swiftlint.yml`
3. Sửa vấn đề hoặc disable rule nếu phù hợp
4. Chạy `swiftlint --fix` cho vấn đề auto-fix được

### Thiếu thư viện hoặc Secrets.xcconfig

Chạy `scripts/download-libs.sh` và `touch Secrets.xcconfig` tại thư mục gốc. Xem [Bước 1](#bước-1-clone-và-bootstrap).

## Bước tiếp theo

<CardGroup cols={2}>
  <Card title="Kiến trúc" icon="sitemap" href="/vi/development/architecture">
    Hiểu cấu trúc codebase
  </Card>
  <Card title="Code Style" icon="code" href="/vi/development/code-style">
    Quy ước code
  </Card>
  <Card title="Build" icon="hammer" href="/vi/development/building">
    Build phiên bản release
  </Card>
  <Card title="GitHub" icon="github" href="https://github.com/datlechin/tablepro">
    Xem mã nguồn
  </Card>
</CardGroup>
