---
title: Giao diện
description: Theme engine với preset có sẵn, theme tùy chỉnh, màu sắc và phông chữ, nhập/xuất
---

# Cài đặt Giao diện

Cấu hình giao diện TablePro trong **Settings** > **Appearance**.

{/* Screenshot: Appearance settings panel */}
<Frame caption="Cài đặt giao diện với trình duyệt theme">
  <img
    className="block dark:hidden"
    src="/images/settings-appearance.png"
    alt="Cài đặt giao diện"
  />
  <img
    className="hidden dark:block"
    src="/images/settings-appearance-dark.png"
    alt="Cài đặt giao diện"
  />
</Frame>

## Theme

Tab Appearance hiển thị giao diện HSplitView chia thành hai phần:

- **Panel trái**: Sidebar danh sách theme, chia thành ba section — "Built-in" (9 theme có sẵn), "Registry" (theme cài từ registry) và "Custom" (theme tùy chỉnh). Mỗi hàng hiển thị thumbnail thu nhỏ, tên theme và nhãn. Thanh công cụ phía dưới có nút `+` (New Theme, Import...), `−` (xóa theme, không khả dụng cho built-in và registry), và biểu tượng gear (Duplicate, Export..., Uninstall cho registry theme).
- **Panel phải**: Hiển thị tên theme đang chọn, kèm segmented picker gồm ba tab: **Fonts**, **Colors** và **Layout**.

### Theme Có sẵn

| Theme | Kiểu | Mô tả |
|-------|------|-------|
| **Default Light** | Light | Giao diện sáng chuẩn theo macOS |
| **Default Dark** | Dark | Giao diện tối chuẩn theo macOS |
| **Dracula** | Dark | Theme tối tông tím theo bảng màu Dracula |
| **Solarized Light** | Light | Bảng màu sáng ấm của Ethan Schoonover |
| **Solarized Dark** | Dark | Bảng màu tối mát của Ethan Schoonover |
| **One Dark** | Dark | Bảng màu One Dark của Atom |
| **GitHub Light** | Light | Màu giao diện sáng GitHub |
| **GitHub Dark** | Dark | Màu giao diện tối GitHub |
| **Nord** | Dark | Bảng màu Bắc Cực, tông xanh dương |

### Giao diện Hệ thống

Mỗi theme có thuộc tính `appearance`: `light`, `dark` hoặc `auto`. Giá trị này quyết định `NSApp.appearance` khi theme được áp dụng. Khi giao diện macOS thay đổi giữa sáng và tối, TablePro chuyển sang theme bạn đã gán cho chế độ đó.

{/* Screenshot: Side-by-side light and dark themes */}
<Frame caption="Chủ đề Sáng vs Tối">
  <img
    className="block dark:hidden"
    src="/images/theme-comparison.png"
    alt="So sánh chủ đề"
  />
  <img
    className="hidden dark:block"
    src="/images/theme-comparison-dark.png"
    alt="So sánh chủ đề"
  />
</Frame>

## Tùy chỉnh Theme

Panel phải có ba tab chỉnh sửa qua segmented picker:

### Fonts

Tab Fonts luôn cho phép chỉnh sửa, kể cả trên theme built-in. Khi bạn thay đổi font của theme built-in, TablePro tự động tạo bản sao (duplicate) để giữ nguyên preset gốc.

Hai cài đặt font:

- **Editor font**: họ font và cỡ chữ cho SQL editor
- **Data grid font**: họ font và cỡ chữ cho lưới kết quả

Mỗi thay đổi hiển thị ngay trên bản xem trước (live preview).

<Tip>
Nếu chỉ muốn đổi font mà không đổi màu, chỉ cần chỉnh tab Fonts — theme sẽ tự duplicate nếu là built-in.
</Tip>

### Colors

5 danh mục màu, mỗi màu dùng hex color picker:

| Danh mục | Kiểm soát |
|----------|-----------|
| **Editor** | Background, text, cursor, selection, line numbers, 8 màu token cú pháp |
| **Data Grid** | 12 thuộc tính màu: ô, nền, NULL placeholder, boolean, hàng xen kẽ, v.v. |
| **Interface** | Text tiers, backgrounds, borders, status, badges |
| **Sidebar** | 5 thuộc tính màu sidebar |
| **Toolbar** | 2 thuộc tính màu toolbar |

Theme built-in không cho chỉnh trực tiếp. Tab Colors hiển thị thông báo "Duplicate to customize" — nhấp để tạo bản sao rồi chỉnh.

### Layout

6 danh mục layout:

| Danh mục | Nội dung |
|----------|----------|
| **Typography** | 7 cỡ chữ |
| **Spacing** | 7 spacing token + list row insets |
| **Icon Sizes** | 9 kích thước icon |
| **Corner Radius** | 3 giá trị bo góc |
| **Row Heights** | 3 chiều cao hàng |
| **Animations** | 4 cài đặt animation |

Cùng cơ chế khóa như Colors: theme built-in cần duplicate trước khi chỉnh.

## Nhập và Xuất

Nhập/xuất theme qua thanh công cụ phía dưới sidebar:

### Xuất Theme

1. Chọn theme trong sidebar
2. Nhấp biểu tượng gear > **Export...**
3. Chọn vị trí lưu

File `.json` xuất ra chứa toàn bộ định nghĩa: màu sắc, font, spacing, layout.

### Nhập Theme

1. Nhấp nút `+` > **Import...**
2. Chọn file theme `.json`
3. Theme xuất hiện trong section "Custom"

<Note>
Theme được lưu tại `~/Library/Application Support/TablePro/Themes/` dưới dạng JSON.
</Note>

## Theme từ Registry

Cài đặt theme từ cộng đồng thông qua plugin registry.

### Cài đặt Theme

1. Vào **Settings > Plugins > Browse**
2. Lọc theo danh mục **Themes**
3. Nhấn **Install Theme**
4. Theme xuất hiện trong section "Registry" của **Settings > Appearance**

Theme từ registry ở chế độ chỉ đọc. Để tùy chỉnh, nhân bản theme trước: bản sao xuất hiện trong section "Custom" và có thể chỉnh sửa.

### Gỡ cài đặt Theme

1. Chọn registry theme trong **Settings > Appearance**
2. Nhấp biểu tượng **gear**
3. Chọn **Uninstall**

Nếu theme đang dùng bị gỡ, TablePro chuyển về Default Light.

<Note>
Registry theme được lưu tại `~/Library/Application Support/TablePro/Themes/Registry/` với tiền tố ID `registry.*`.
</Note>

## Màu Kết nối

Gán màu cho kết nối để tổ chức trực quan:

| Màu | Đề xuất |
|-------|---------------|
| None | Mặc định, trung tính |
| Red | Database Production |
| Orange | Môi trường Staging |
| Yellow | Database Testing |
| Green | Development/local |
| Blue | Database Shared |
| Purple | Mục đích đặc biệt |
| Pink | Dự án cá nhân |

### Đặt Màu Kết nối

1. Mở form kết nối (chỉnh sửa hoặc tạo mới)
2. Trong phần **Appearance**, nhấp **Color**
3. Chọn màu từ bảng màu
4. Lưu kết nối

{/* Screenshot: Connection color picker */}
<Frame caption="Bộ chọn màu kết nối">
  <img
    className="block dark:hidden"
    src="/images/connection-color-picker.png"
    alt="Màu kết nối"
  />
  <img
    className="hidden dark:block"
    src="/images/connection-color-picker-dark.png"
    alt="Màu kết nối"
  />
</Frame>

### Hiển thị Màu Kết nối

Màu kết nối hiện ở:

- Danh sách kết nối trong sidebar
- Tiêu đề tab
- Thanh trạng thái kết nối
- Tiêu đề cửa sổ (khi đã kết nối)

<Warning>
Dùng đỏ cho database production như nhắc nhở cẩn thận khi chạy query.
</Warning>

{/* Screenshot: Màu kết nối trong sidebar và tab */}
<Frame caption="Màu kết nối hiển thị trong sidebar và tab">
  <img
    className="block dark:hidden"
    src="/images/connection-colors-sidebar.png"
    alt="Màu kết nối trong sidebar và tab"
  />
  <img
    className="hidden dark:block"
    src="/images/connection-colors-sidebar-dark.png"
    alt="Màu kết nối trong sidebar và tab"
  />
</Frame>

## Màu Loại Database

Mỗi loại database có màu cố định để nhận diện nhanh:

| Database | Màu |
|----------|-------|
| MySQL | Cam |
| MariaDB | Cyan |
| PostgreSQL | Xanh dương |
| SQLite | Xanh lá |

Hiển thị trên:

- Biểu tượng loại database
- Chỉ báo loại kết nối
- Biểu tượng sidebar

{/* Screenshot: Màu loại cơ sở dữ liệu */}
<Frame caption="Mã màu loại database: MySQL, MariaDB, PostgreSQL, SQLite">
  <img
    className="block dark:hidden"
    src="/images/database-type-colors.png"
    alt="Màu loại cơ sở dữ liệu"
  />
  <img
    className="hidden dark:block"
    src="/images/database-type-colors-dark.png"
    alt="Màu loại cơ sở dữ liệu"
  />
</Frame>

## Chủ đề Editor

Màu tô sáng cú pháp SQL do theme đang dùng quyết định. Ví dụ, trong Default Dark:

| Phần tử | Màu |
|---------|-------|
| Keywords | Hồng |
| Strings | Xanh lá |
| Numbers | Xanh dương |
| Comments | Xám |
| Identifiers | Màu văn bản mặc định |

Chuyển theme sẽ thay đổi màu cú pháp. Tùy chỉnh bất kỳ màu nào qua tab Colors của theme.

## Phần tử UI

### Màu Trạng thái

| Màu | Ý nghĩa |
|-------|---------|
| Green | Thành công, đã kết nối, hợp lệ |
| Orange | Cảnh báo, đang xử lý, đang kết nối |
| Red | Lỗi, thất bại, không hợp lệ |
| Blue | Thông tin |
| Gray | Trung tính, đã ngắt kết nối |

### Màu Kiểu Dữ liệu

Trong bảng dữ liệu:

| Loại | Hiển thị |
|------|---------|
| NULL | Xám, in nghiêng |
| Boolean True | Xanh lá |
| Boolean False | Đỏ |
| Numbers | Màu tiêu chuẩn |
| Text | Màu tiêu chuẩn |

## Khắc phục Sự cố

- **Theme không thay đổi**: Khởi động lại TablePro. Nếu dùng chế độ auto, kiểm tra giao diện macOS trong System Settings.
- **Màu hiển thị sai**: Kiểm tra color profile và hiệu chỉnh màn hình trong System Settings. True Tone có thể ảnh hưởng trên màn hình hỗ trợ.
- **Theme tùy chỉnh bị mất**: Kiểm tra thư mục `~/Library/Application Support/TablePro/Themes/` — file JSON của theme phải có ở đó.

## Cài đặt Liên quan

<CardGroup cols={2}>
  <Card title="Cài đặt Editor" icon="code" href="/vi/customization/editor-settings">
    Font và giao diện editor
  </Card>
  <Card title="Tổng quan Cài đặt" icon="gear" href="/vi/customization/settings">
    Tất cả danh mục cài đặt
  </Card>
  <Card title="Bảng Dữ liệu" icon="table" href="/vi/features/data-grid">
    Cấu hình hiển thị dữ liệu
  </Card>
  <Card title="Kết nối" icon="plug" href="/vi/databases/overview">
    Cài đặt màu kết nối
  </Card>
</CardGroup>
